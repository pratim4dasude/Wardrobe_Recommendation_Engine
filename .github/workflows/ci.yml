#name: CI
#
#on:
#  push:
#    branches:
#      - main
#      - master
#
#  pull_request:
#    branches:
#      - main
#      - master
#
#jobs:
#  test:
#    name: Run tests and lint checks
#    runs-on: ubuntu-latest
#
#    steps:
#      - name: Checkout repository
#        uses: actions/checkout@v4
#
#      - name: Set up Python
#        uses: actions/setup-python@v5
#        with:
#          python-version: "3.11"
#
#      - name: Install test dependencies
#        run: |
#          python -m pip install --upgrade pip
#          pip install pytest ruff
#
#      - name: Run lint check
#        run: |
#          ruff check .
#
#      - name: Run tests
#        run: |
#          pytest

name: CI

on:
  push:
    branches:
      - main
      - master

  pull_request:
    branches:
      - main
      - master

jobs:
  test:
    name: Run tests and lint checks
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install test dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest ruff

      - name: Run lint check
        run: |
          ruff check .

      - name: Run tests if available
        run: |
          if [ -d "tests" ]; then pytest; else echo "< ----- TESTING --- >"; fi